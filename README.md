# EP4-SQL-MaosDoAmanha

# Experiência Prática IV — Banco de Dados SQL (MySQL)

Este repositório contém todos os scripts SQL desenvolvidos para o projeto **Mãos do Amanhã**, incluindo criação de tabelas, inserção de dados, consultas, atualizações e exclusões.

---

## 📁 Estrutura do Repositório

/EP4-SQL
│
├── create_tables.sql

├── insert_data.sql

├── queries.sql

├── updates.sql

├── deletes.sql



---

## 🚀 Como Executar

1. Abra o **MySQL Workbench**.
2. Execute **create_tables.sql** (cria o banco e todas as tabelas).
3. Execute **insert_data.sql** (povoa o banco).
4. Use os scripts:
   - `queries.sql`
   - `updates.sql`
   - `deletes.sql`
  

---

## 🏗 Modelo

- MySQL 8+
- Engine InnoDB  
- Integridade referencial completa  
- Chaves compostas quando necessário  

---

## 📌 Scripts Incluídos

- Criação de banco e tabelas (`create_tables.sql`)
- Inserts coerentes com o minimundo (`insert_data.sql`)
- Consultas SQL (JOIN, WHERE, LIMIT, ORDER BY)
- Updates com segurança (WHERE)
- Deletes que respeitam FKs

---

## Projeto acadêmico
By
## José Ferreira
